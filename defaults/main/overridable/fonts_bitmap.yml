# NOTE: when a font is installed, its accompanying font name vars are exported

# Bitmap font with most complete Unicode coverage of any font
# See http://unifoundry.com/unifont/index.html
cfg_fonts_install_unifont_font: false

# Bitmap font designed for heavy terminal use
# See http://terminus-font.sourceforge.net/
cfg_fonts_install_terminus_font: false

